#include "main.h"
#include <stdio.h>

/**
 * _strspn - gets the negth of a prefix substring
 * @s: the string
 * @accept: the prefix
 * Return: the number of bytes in the initial segment of s
 * which consists only of bytes from accept
 */
