#include "main.h"

/**
 * _islower - checks whether a character is lowercase or not
 * @c: the character to check
 * Return: 1 if chharacter is lowercase, 0 if not
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
