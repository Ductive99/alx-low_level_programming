#include "main.h"

/**
 * _islower - checks whether a character is lowercase or not
 * Return: 1 if chharacter is lowercase
 * Return: 0 if not
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
