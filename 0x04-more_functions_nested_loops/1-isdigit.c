#include "main.h"

/**
 * _isdigit - checks if a character is a digit (0 to 9)
 * @c: the character to check
 * Return: 1 if digit, 0 if not
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
