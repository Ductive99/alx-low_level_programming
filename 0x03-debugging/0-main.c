#include "main.h"

/**
 * main - tests the fumction positive_or_negative
 * @i: the integer equal to 0 to check
 * Return: 0 when ran properly
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
